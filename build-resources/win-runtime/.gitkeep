# Staged at build time by scripts/bundle-vc-runtime.mjs (MSIX app-local CRT).
# The DLLs are git-ignored; this file just keeps the directory present so the
# NSIS build (which reads win.extraFiles) never fails on a missing folder.
